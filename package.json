{
  "name": "business-card-generator",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf output/** && node src/index.js",
    "cluster": "rm -rf output/** && node src/puppeteerCluster.js"
  },
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^5.3.0",
    "puppeteer-cluster": "^0.22.0",
    "uuid": "^8.3.0"
  }
}
